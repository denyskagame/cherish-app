# ── Phase 0 (required for the wedding) ───────────────────────────
DATABASE_URL="postgresql://USER:PASS@HOST:5432/DB?schema=public"
DIRECT_URL="postgresql://USER:PASS@HOST:5432/DB?schema=public"   # migrations
NEXT_PUBLIC_APP_URL="http://localhost:3000"

ANTHROPIC_API_KEY="sk-ant-..."
ANTHROPIC_MODEL="claude-sonnet-4-6"

ENCRYPTION_KEY="<64 hex chars>"   # node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

ADMIN_PASSWORD="<a long passphrase>"       # single-gate admin (Phase 0)
ADMIN_COOKIE_SECRET="<32+ random chars>"   # signs the admin session cookie

GOOGLE_CLIENT_ID="....apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="GOCSPX-..."
GOOGLE_REDIRECT_URI="http://localhost:3000/api/auth/google/callback"

EVENT_SLUG="sarah-michael-2026"            # the one wedding (Phase 0 convenience)

# ── Phase 1+ (leave blank for the wedding) ───────────────────────
# NEXT_PUBLIC_SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
# R2_ACCOUNT_ID=
# STRIPE_SECRET_KEY=
# UPSTASH_REDIS_REST_URL=
# INNGEST_EVENT_KEY=
# SENTRY_DSN=
